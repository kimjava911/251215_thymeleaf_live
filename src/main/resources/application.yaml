server:
  port: 8080
spring:
  application:
    name: 251215_upload-exception-thymeleaf
  profiles:
    active: dev

  # 파일 업로드
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 30MB
      file-size-threshold: 2KB

file:
  upload-dir: uploads